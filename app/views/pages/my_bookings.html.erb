<h1> Voici la liste de tes réservations</h1>

<p> <%= @my_bookings.count %> réservations </p>

<% @my_bookings.each do |booking| %>
  <h3><%= booking.product.name %></h3>
  <p>Description :<%= booking.product.description %></p>
  <p>Catégorie :<%= booking.product.category %></p>
  <p>Début de la Réservation : <%= booking.start_date %></p>
  <p>Fin de la Réservation : <%= booking.end_date %></p>
  <p>Prix total : <%=(booking.end_date - booking.start_date).to_i * booking.product.price %> €</p>

  <h3>Coordonnées du loueur</h3>
  <p><%= booking.product.user.first_name %> <%= booking.product.user.last_name %></p>
  <p><%= booking.product.address %></p>
  <%= link_to "Annuler cette réservation",
    booking_path(booking),
    method: :delete,
    data: { confirm: "Êtes-vous sûr?" } %>

<% end %>
